the paramiko library is required to make ssh connections to the instances

1) install homebrew
2) install pip
3) install paramiko via pip

ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
brew install pip
sudo pip install paramiko